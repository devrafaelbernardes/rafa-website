import { BASE_URL } from './URLs';

export const HomeURL = () => {
    return {
        ROUTER : {
            BASE : BASE_URL,
        },
        REDIRECT : {
            BASE : BASE_URL,
        }
    };
};